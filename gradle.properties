# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx1G
org.gradle.java.home=C:\\Users\\trasa\\.jdks\\openjdk-25.0.1
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

#read more on this at https://github.com/neoforged/ModDevGradle?tab=readme-ov-file#better-minecraft-parameter-names--javadoc-parchment
# you can also find the latest versions at: https://parchmentmc.org/docs/getting-started
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17
# Environment Properties
# You can find the latest versions here: https://projects.neoforged.net/neoforged/neoforge
# The Minecraft version must agree with the Neo version to get a valid artifact
minecraft_version=1.21.1
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.21.1]
# The Neo version must agree with the Minecraft version to get a valid artifact
neo_version=21.1.248
# The loader version range can only use the major version of FML as bounds
loader_version_range=[1,)

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=unlimitedspace
# The human-readable display name for the mod.
mod_name=Unlimited Space
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=All Rights Reserved
# The mod version. See https://semver.org/
mod_version=1.0.0
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=com.modscreating.unlimitedspace

## Creating Space 1.7.20 / Create ecosystem dependencies
# Versions mirrored from the official Creating Space 1.7.20 (RealAntEngineer/creating_space, branch "1.21.1-release")
# These are the exact versions Creating Space 1.7.20 (builds for MC 1.21.1 + NeoForge) was compiled against.
# Create: a hard (mandatory) runtime dependency declared in Creating Space's neoforge.mods.toml
create_version=6.0.9-216
# Flywheel: rendering library used by Create (API is compileOnly, full implementation is runtimeOnly)
flywheel_version=1.0.6
# Ponder: Create's guide overlay library, split out into its own artifact for 1.21.1
ponder_version=1.0.81
# Registrate: registration library used by Create / Creating Space
registrate_version=MC1.21-1.3.0+67
# JEI: optional ingredient/recipe integration (API compileOnly, full jar runtimeOnly when used in dev)
jei_version=19.21.0.247
# Creating Space is distributed via Modrinth Maven (mod id "creating-space").
# NOTE: "1.7.20" is the mod_version of the official `1.21.1-release` source branch (RealAntEngineer/creating_space),
# but that version is NOT yet published to Modrinth. The latest published release for MC 1.21.1 + NeoForge is 1.7.18,
# so we build against that released artifact (API/interfaces are identical to the 1.7.20 branch).
# The addon's neoforge.mods.toml should declare a broad "creatingspace" version range to allow 1.7.20+ once it ships.
creatingspace_version=1.7.18
## DynamicDimensions local artifact (R14.3) - authoritative local build, no Maven
# Directory containing the built DynamicDimensions NeoForge 0.9.1 jar + badpackets runtime dep.
dynamicdimensions_dir=C:\\Users\\trasa\\OneDrive\\Desktop\\DynamicDimensions
dynamicdimensions_version=0.9.1+75e1622e-dirty
